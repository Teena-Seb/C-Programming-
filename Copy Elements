//Write a program in C to copy the elements of one array into another array.

#include<stdio.h>
int main(){
    int n,i,s[100],d[100];
    scanf("%d",&n);
    for (i=0; i<n; i++)
        scanf("%d",&s[i]);
    for (i=0; i<n; i++)
        d[i]=s[i];
    for (i=0; i<n; i++)
        printf("%d ",d[i]);
    return 0;
}
