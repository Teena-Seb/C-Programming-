//Write a program in C to read 'n' values of an array and display it in reverse order.

#include<stdio.h>
int main(){
    int n,i,num[100];
    scanf("%d",&n);
    for(i=0; i<n;i++)
        scanf("%d",&num[i]);
    for(i=n; i!=0; i--)
    printf("%d ",num[i-1]);
    return 0;
}
